# Shell script defined by starting from a defined shell and then executing from thjat shell, all contained
# within a script.

VARIABLE="Value" - sets the variable
export VARIABLE - exports it to env - the two can be combined like export VARIABLE="Value"

Can also use the output of a command to define a variable, such as:
	export TODAY=`date`



