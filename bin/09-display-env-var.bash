#!/bin/bash

# LA exercise 1 - create a script that, when run, will display the following environment variables to the console.
#
# USER
# HOME
# HISTCONTROL
# TERM

echo "$USER <- This is the USER environment variable."
echo "$HOME <- This is the HOME environment variable." 
echo "$HISTCONTROL <- This is the HISTCONTROL environment variable."
echo "$TERM <- This is the TERM environment variable."
